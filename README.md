# Cacablu
Creative Audio-visual Composition And Behavioral Logic Unit

Workspace layout:
- `src/` contains the current application.
- `apps/studio/` is the sandbox for the new timeline UI.
- `packages/timeline/` is the reusable timeline package.
